maybe we don't even need functions. instead, the user can try to call ASTs directly?
	skip the compilation step.
nah, we need to copy. you can't get a reference to an actual fixed function without the promise of immutness.